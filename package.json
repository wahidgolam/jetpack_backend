{
  "scripts": {
    "emu": "npm --prefix functions run build && firebase emulators:start"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.3",
    "typescript": "^4.0.0"
  },
  "dependencies": {
    "@google-cloud/tasks": "^5.5.2",
    "axios": "^1.7.7",
    "dotenv": "^16.4.7",
    "mongodb": "^6.12.0"
  }
}
